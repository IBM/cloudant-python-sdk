[tox]
envlist = py37-lint, py35, py36, py37, py38

[testenv:py37-lint]
basepython = python3.7
deps = pylint
commands = pylint --rcfile=.pylintrc ibmcloudant test

[testenv]
passenv = TOXENV CI TRAVIS*
commands =
         py.test --reruns 3 -o junit_family=xunit2 --junitxml=test-reports/junit-{envname}.xml --cov=ibmcloudant {posargs}
         codecov -e TOXENV
deps =
     -r{toxinidir}/requirements.txt
     -r{toxinidir}/requirements-dev.txt
usedevelop = True
exclude = .venv,.git,.tox,docs
